/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ["127.0.0.1", "127.0.0.1:19000", "localhost", "localhost:19000"],
  devIndicators: false
};

export default nextConfig;

